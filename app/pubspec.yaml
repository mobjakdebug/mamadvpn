name: mamadvpn
description: "MamadVPN - TCP Desync Censorship Circumvention"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter

  # FFI for calling Rust native library
  ffi: ^2.1.0

  # Cupertino icons
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # Bundled config JSON
  assets:
    - assets/config.json
